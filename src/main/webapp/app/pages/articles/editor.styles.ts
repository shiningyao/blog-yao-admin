import styled from 'styled-components';
import { PageWrapper } from '@/pages/styles';

export const EditorPageWrapper = styled(PageWrapper)`
    .card-body {
        font-size: 16px;
        font-family: 'Roboto Slab',serif;
    }
`;

export default {
    EditorPageWrapper
};